{
  "authorized_keys": [
    {
      "key": "{{ lookup('file', '../uat/ansible/ssh-keys/alan-verbner.pub') }}"
    },
    {
      "key": "{{ lookup('file', './gitlabci.pub') }}"
    },
    {
      "key": "{{ lookup('file', '../uat/ansible/ssh-keys/eric-stoppel.pub') }}"
    }
  ]
}
